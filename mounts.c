#include "mounts.h"

/*
 * Finds information about each mounted fs, and returns it in an array
 */

device_t *get_devices() {
  device_t *devices = malloc(10 * sizeof(device_t));

  
} /* get_devices(); */